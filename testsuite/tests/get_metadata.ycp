// Sysconfig module - testsuite
//
// get_metadata tests
//
// $Id$
//

{

include "testsuite.ycp";

import "Pkg";   // override packamanager

import "Mode";		// mode is imported in Product
Mode::config = true;	// package manager is initialized in Product constructor when Mode::config is false !!
import "Sysconfig";

TEST( ``(Sysconfig::get_metadata(nil) ), [ $[], $[], $[] ], nil );
TEST( ``(Sysconfig::get_metadata("") ), [ $[], $[], $[] ], nil );
TEST( ``(Sysconfig::get_metadata(" ## Type: ip  ") ), [ $[], $[], $[] ], nil );

TEST( ``(Sysconfig::get_metadata("## Type: ip  ") ), [ $[], $[], $[] ], nil );
TEST( ``(Sysconfig::get_metadata("##Type: ip") ), [ $[], $[], $[] ], nil );
TEST( ``(Sysconfig::get_metadata("## Type: sdf  \n## Default: sdf") ), [ $[], $[], $[] ], nil );
TEST( ``(Sysconfig::get_metadata("\n\n## Type\n   \n##Def\n  ##Path") ), [ $[], $[], $[] ], nil );

}

